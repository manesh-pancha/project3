@model manga2.Models.mangaseries

@{
    ViewBag.Title = "Delete";
}
<link href="~/Content/CSS/character bar.css" rel="stylesheet" />
<link href="~/Content/CSS/stylingC.css" rel="stylesheet" />
<h2>Delete</h2>

<h3>Are you sure you want to delete this record?</h3>
<h4> series</h4>

<dl class="dl-horizontal">
    <dt>@Html.LabelFor(m => m.image)</dt>
    <dd>@Html.EditorFor(m => m.image)</dd>

    <dt>@Html.LabelFor(m => m.author)</dt>
    <dd>@Html.EditorFor(m => m.author)</dd>

    <dt>@Html.LabelFor(m => m.manga_series)</dt>
    <dd>@Html.EditorFor(m => m.manga_series)</dd>

    <dt>@Html.LabelFor(m => m.release)</dt>
    <dd>@Html.EditorFor(m => m.release)</dd>

    <dt>@Html.LabelFor(m => m.status)</dt>
    <dd>@Html.EditorFor(m => m.status)</dd>

</dl>
@using (Html.BeginForm())
{
    @Html.AntiForgeryToken()
    <div class="form-group">
        <div class="col-sm-offset-1 col-sm-11">
            <input type="submit" class="btn btn-default" value="Delete Record" />
        </div>
    </div>
}
@Html.ActionLink("Back to List","Index")

